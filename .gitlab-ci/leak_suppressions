# Suppress leaks from glib-networking
leak:libgiognutls.so
leak:libgnutls.so

# FIXME: I'm not sure these are real.
# It says that `soup_test_session_new()` leaks but the last call to all of them asserts the refcount is 1 and unrefs.
leak:do_tls_interaction_test
leak:do_tls_interaction_msg_test
leak:do_tls_interaction_preconnect_test
