image: fedora:28

build_stable:
  before_script:
    - dnf update -y --nopgpcheck
    - dnf install 'dnf-command(builddep)'
    - dnf builddep -y --nopgpcheck libsoup
  script:
    - ./autogen.sh
    - make check
